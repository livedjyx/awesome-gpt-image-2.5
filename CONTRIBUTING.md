# 贡献内容

新增选题前，先检查 [分类目录](README.md#分类目录) 中是否已有相同用途、风格和画面机制。更换人物、城市或产品名称通常作为同一条目的变量处理。

提交可独立使用的提示词、对应结果图和生成记录。输入素材须具有发布权限；内容贡献按 [CC BY 4.0](docs/licensing.md) 发布，代码贡献按 MIT 发布。

1. 在 `data/catalog.json` 中登记新编号或选用已有编号，保持编号唯一。
2. 按 [条目模板](templates/README.md) 在对应分类下建立条目目录。
3. 在 `prompt.md` 保存实际提交的提示词，准备必要输入，并生成 `result-01`。
4. 填写图片及提示词的 SHA-256、实际模型与参数；按 [条目规范](docs/entry-format.md) 完成审核。
5. 运行构建、校验与测试，确认页面、图片和状态一致后再提交。

目前使用 JSON 保存可校验数据，Markdown 页面由脚本生成。修改选题时编辑 `data/catalog.json`，修改条目时编辑 `entry.json` 和 `prompt.md`，随后运行 `python scripts/catalog.py build`。

提交说明应概述新增的场景、解决的问题及检查结果。草稿和未通过审核的记录不计入正式图库。
